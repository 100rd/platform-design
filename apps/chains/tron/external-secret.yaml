apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: tron-node-credentials
  namespace: tron
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: tron-node-credentials
    creationPolicy: Owner
  data:
    - secretKey: node-key
      remoteRef:
        key: /chains/tron/node-credentials
        property: node-key
