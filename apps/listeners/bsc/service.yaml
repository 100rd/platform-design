apiVersion: v1
kind: Service
metadata:
  name: bsc-listener
  namespace: bsc
  labels:
    app.kubernetes.io/name: bsc-listener
    app.kubernetes.io/component: listener
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bsc-listener
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: metrics
      port: 9090
      targetPort: metrics
