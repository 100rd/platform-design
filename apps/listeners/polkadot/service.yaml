apiVersion: v1
kind: Service
metadata:
  name: polkadot-listener
  namespace: polkadot
  labels:
    app.kubernetes.io/name: polkadot-listener
    app.kubernetes.io/component: listener
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: polkadot-listener
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: metrics
      port: 9090
      targetPort: metrics
