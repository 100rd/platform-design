apiVersion: apps/v1
kind: Deployment
metadata:
  name: polkadot-listener
  namespace: polkadot
  labels:
    app.kubernetes.io/name: polkadot-listener
    app.kubernetes.io/component: listener
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: polkadot-listener
  template:
    metadata:
      labels:
        app.kubernetes.io/name: polkadot-listener
        app.kubernetes.io/component: listener
        blockchain.io/network: polkadot
    spec:
      serviceAccountName: polkadot-listener
      automountServiceAccountToken: false

      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: polkadot-listener

      containers:
        - name: listener
          image: internal/polkadot-listener:latest
          ports:
            - name: grpc
              containerPort: 50051
            - name: metrics
              containerPort: 9090
          env:
            - name: CHAIN_RPC_URL
              value: "http://polkadot-rpc.polkadot.svc.cluster.local:9933"
            - name: CHAIN_WS_URL
              value: "ws://polkadot-rpc.polkadot.svc.cluster.local:9944"
            - name: BLOCK_CONFIRMATIONS
              valueFrom:
                configMapKeyRef:
                  name: polkadot-listener-config
                  key: block-confirmations
            - name: CHAIN_ID
              valueFrom:
                configMapKeyRef:
                  name: polkadot-listener-config
                  key: chain-id
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp

      volumes:
        - name: tmp
          emptyDir: {}
