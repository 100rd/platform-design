apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: tron-listener-policy
  namespace: tron
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: tron-listener
  ingress:
    # gRPC and metrics from within the cluster only
    - fromEndpoints:
        - {}
      toPorts:
        - ports:
            - port: "50052"
              protocol: TCP
            - port: "9090"
              protocol: TCP
  egress:
    # Connect to Tron gRPC and HTTP API within the cluster
    - toEndpoints:
        - matchLabels:
            app.kubernetes.io/name: tron
      toPorts:
        - ports:
            - port: "50051"
              protocol: TCP
            - port: "8090"
              protocol: TCP
    # DNS resolution
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP
