apiVersion: v1
kind: Service
metadata:
  name: cosmos-listener
  namespace: cosmos
  labels:
    app.kubernetes.io/name: cosmos-listener
    app.kubernetes.io/component: listener
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cosmos-listener
  ports:
    - name: grpc
      port: 50051
      targetPort: grpc
    - name: metrics
      port: 9090
      targetPort: metrics
