apiVersion: apps/v1
kind: Deployment
metadata:
  name: cosmos-listener
  namespace: cosmos
  labels:
    app.kubernetes.io/name: cosmos-listener
    app.kubernetes.io/component: listener
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cosmos-listener
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cosmos-listener
        app.kubernetes.io/component: listener
        blockchain.io/network: cosmos
    spec:
      serviceAccountName: cosmos-listener
      automountServiceAccountToken: false

      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        runAsGroup: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault

      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: cosmos-listener

      containers:
        - name: listener
          image: internal/cosmos-listener:latest
          ports:
            - name: grpc
              containerPort: 50051
            - name: metrics
              containerPort: 9090
          env:
            - name: CHAIN_RPC_URL
              value: "http://cosmos-rpc.cosmos.svc.cluster.local:26657"
            - name: CHAIN_GRPC_URL
              value: "cosmos-rpc.cosmos.svc.cluster.local:9090"
            - name: CHAIN_API_URL
              value: "http://cosmos-rpc.cosmos.svc.cluster.local:1317"
            - name: BLOCK_CONFIRMATIONS
              valueFrom:
                configMapKeyRef:
                  name: cosmos-listener-config
                  key: block-confirmations
            - name: CHAIN_ID
              valueFrom:
                configMapKeyRef:
                  name: cosmos-listener-config
                  key: chain-id
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          livenessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
          volumeMounts:
            - name: tmp
              mountPath: /tmp

      volumes:
        - name: tmp
          emptyDir: {}
