apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chains-staging
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: chains:staging
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: chains
    env: staging
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      path: apps/chains
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/staging/values/chains/values.yaml
        values: |
          environment: "staging"
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: staging-chains
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
