apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mono-staging
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: mono:staging
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: mono
    env: staging
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      path: apps/mono
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/staging/values/mono/values.yaml
        values: |
          environment: "staging"
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: staging-mono
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
