apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mono-integration
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: mono:integration
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: mono
    env: integration
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      path: apps/mono
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/integration/values/mono/values.yaml
        values: |
          environment: "integration"
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: integration-mono
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
