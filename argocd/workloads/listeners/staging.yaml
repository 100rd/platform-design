apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: listeners-staging
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: listeners:staging
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: listeners
    env: staging
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      path: apps/listeners
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/staging/values/listeners/values.yaml
        values: |
          environment: "staging"
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: staging-listeners
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
