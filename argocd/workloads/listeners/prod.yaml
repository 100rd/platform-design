apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: listeners-prod
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: listeners:prod
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: listeners
    env: prod
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      path: apps/listeners
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/prod/values/listeners/values.yaml
        values: |
          environment: "prod"
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: prod-listeners
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
