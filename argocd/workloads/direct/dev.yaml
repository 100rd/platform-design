apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: direct-dev
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: direct:dev
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: direct
    env: dev
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      path: apps/direct
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/dev/values/direct/values.yaml
        values: |
          environment: "dev"
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dev-direct
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
