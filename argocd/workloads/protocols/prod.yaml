apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: protocols-prod
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: protocols:prod
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: protocols
    env: prod
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      path: apps/protocols
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/prod/values/protocols/values.yaml
        values: |
          environment: "prod"
    - repoURL: git@github.com:100rd/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: prod-protocols
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
