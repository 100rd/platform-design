apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: protocols-integration
  namespace: argocd
  annotations:
    kargo.akuity.io/authorized-stage: protocols:integration
  labels:
    app.kubernetes.io/part-of: platform-workloads
    app.kubernetes.io/component: protocols
    env: integration
spec:
  project: platform-workloads
  sources:
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      path: apps/protocols
      helm:
        ignoreMissingValueFiles: true
        valueFiles:
          - $values/envs/integration/values/protocols/values.yaml
        values: |
          environment: "integration"
    - repoURL: git@github.com:IgorGerasimow/platform-design.git
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: integration-protocols
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
