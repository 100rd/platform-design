apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../cluster-envs/integration

commonLabels:
  cluster-role: velocity

# Add role+env specific patches here
# patches:
#   - path: patches/custom.yaml
