apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

commonLabels:
  env: stage

patches: []
# Add stage-specific patches in the patches/ directory, e.g.:
# - path: patches/resource-limits.yaml
#   target:
#     kind: Deployment
