apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base environment configuration shared across all environments.
# Individual env overlays (dev/, stage/, etc.) reference this as a base
# and apply environment-specific patches.
#
# Add common resources, configmaps, or labels here.

commonLabels:
  managed-by: argocd

# Add base resources as they are created
# resources: []
